package com.omvoid.community.corexp;

import com.omvoid.community.Edge;
import com.omvoid.community.Vertex;
import com.omvoid.community.VertexWeightProcessor;
import org.jgrapht.Graph;

public class DefaultVertexWeightProcessor implements VertexWeightProcessor {
    public void compute(Graph<Vertex, Edge> graph) {

    }
}
